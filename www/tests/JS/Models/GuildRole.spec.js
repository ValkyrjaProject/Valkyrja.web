import sinon from "sinon";
import {expect} from "chai";
import {GuildRole, createGuildRole} from "../../../resources/assets/js/models/GuildRole";

describe("GuildRole", function () {
    it("should take 'id' and 'name' parameters in constructor");

    it("should parse input to 'permission_level' as integer");

    it("should return name when calling toString()");
});

describe("createGuildRole", function () {
    it("creates GuildRole as freezed object");

    it("returns created GuildRole");
});
